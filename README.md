# ShiftLog

Internal tool for recording and reviewing shift activity across two sites.
React frontend (`web/`), Express API (`server/`), Postgres database.

Start here: [Problem_statement.md](Problem_statement.md)
